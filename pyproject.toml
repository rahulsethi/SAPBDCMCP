# File: pyproject.toml
<<<<<<< HEAD
# Version: v6
=======
# Version: v5
>>>>>>> 0ed8e4279334917c478c9a14d71904c3ea4c961e

[project]
name = "sap-bdc-mcp"
version = "0.2.0"
description = "Model Context Protocol (MCP) server for SAP Business Data Cloud (BDC): contract-first, safe-by-default tooling for AI agents."
readme = "README.md"
requires-python = ">=3.11"
# v0.2.0 relicensed from MIT to PolyForm Noncommercial 1.0.0.
# Commercial use requires a separate license — see docs_public/COMMERCIAL_LICENSING.md.
license = { text = "PolyForm Noncommercial License 1.0.0" }

authors = [
  { name = "Rahul Sethi" }
]

keywords = [
  "mcp",
  "sap",
  "business-data-cloud",
  "bdc",
  "ord",
  "csn",
  "agentic",
  "ai-tools"
]

classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: Other/Proprietary License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Software Development :: Libraries",
  "Topic :: Scientific/Engineering :: Information Analysis",
]

dependencies = [
  "mcp[cli]>=1.25.0",
  "pydantic>=2.7.0",
  "httpx>=0.27.0",
  "python-dotenv>=1.0.1",
  "jsonschema>=4.20.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
  "pytest-cov>=5.0.0",
  "ruff>=0.6.0",
  "mypy>=1.10.0",
  "pre-commit>=3.7.0",
  "build>=1.2.2",
  "twine>=5.1.1",
  "types-jsonschema>=4.20.0",
  "pytest-asyncio>=0.23.0",
]

[project.scripts]
sap-bdc-mcp = "sap_bdc_mcp.__main__:main"

[project.urls]
Homepage = "https://github.com/rahulsethi/SAPBDCMCP"
Repository = "https://github.com/rahulsethi/SAPBDCMCP"
Issues = "https://github.com/rahulsethi/SAPBDCMCP/issues"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
asyncio_mode = "auto"

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

# Ensure JSON schemas are included in wheels
[tool.setuptools.package-data]
sap_bdc_mcp = ["schemas/**/*.json"]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
